@model IEnumerable<GestionDocumental.Data.CheckType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Active)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Check.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TypeProcess.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Active)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Check.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TypeProcess.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCheckType }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCheckType }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCheckType })
        </td>
    </tr>
}

</table>
